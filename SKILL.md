---
name: cache-optimizer
description: Use in every conversation to optimize token usage, reduce costs, and improve response efficiency. Applies to all scenarios including coding, Q&A, document processing, and general chat.
---

# 缓存优化器（Cache Optimizer）

## 概述

本 Skill 融合了 2025-2026 年最新的 **Context Engineering（上下文工程）** 方法论，提供一套通用的 Token 优化策略。核心思想已从"写好提示词"升级为"**用恰到好处的信息填满上下文窗口**"。

**核心原则：** 用最少的 Token 传递最多的信息

**预估节省：** 日常对话 20-40%，长文档场景 50-70%

---

## 一、Context Engineering 核心思想

### 1.1 从 Prompt Engineering 到 Context Engineering

| 维度 | Prompt Engineering | Context Engineering |
|------|-------------------|-------------------|
| 关注点 | 怎么写指令 | 上下文窗口里放什么信息 |
| 策略 | 优化提示词措辞 | 优化信息选择与组织 |
| 效果上限 | 10-20% | 40-70% |
| 核心问题 | "怎么说清楚" | "什么该放进来，什么不该放" |

### 1.2 上下文窗口的"黄金法则"

```
┌─────────────────────────────────────────┐
│  上下文窗口分配策略                       │
├─────────────────────────────────────────┤
│  40% 核心信息（当前任务必需）              │
│  30% 参考信息（相关背景）                  │
│  20% 指令/格式（如何输出）                 │
│  10% 冗余缓冲（避免截断）                  │
└─────────────────────────────────────────┘
```

**关键洞察：** 输入越长，模型性能并非线性提升——超过一定阈值后，**Context Rot（上下文腐烂）** 会导致性能下降。更多 Token ≠ 更好结果。

---

## 二、Prompt Caching 利用策略

### 2.1 什么是 Prompt Caching

OpenAI、Claude、阿里云百炼等平台均支持 **Prompt Caching**：当请求包含重复内容时，系统自动复用缓存结果，**命中部分按 10% 计费**。

### 2.2 如何最大化缓存命中率

| 策略 | 说明 | 节省效果 |
|------|------|----------|
| **固定系统提示** | 每次对话使用相同的系统提示前缀 | 命中率最高 |
| **公共前缀前置** | 将重复内容放在 messages 最前面 | 提高匹配概率 |
| **避免微小变化** | 相同内容保持完全一致的措辞 | 避免缓存失效 |
| **缓存标记** | 在长文本后添加 cache_control 标记 | 显式控制缓存 |

### 2.3 缓存友好的 Prompt 结构

```markdown
❌ 不友好（每次变化）：
"帮我分析这段代码，用中文回答，输出格式为列表..."

✅ 友好（固定前缀）：
[固定系统提示] → [变化的问题部分]
这样系统提示部分始终命中缓存
```

---

## 三、历史对话压缩

### 3.1 对话摘要压缩法

当对话超过 5 轮时，将历史对话压缩为摘要，可**再省 30% Token**。

```markdown
原始对话（500 Token）：
用户：帮我写一个 Python 爬虫
助手：好的，以下是 requests + BeautifulSoup 的示例...
用户：能不能改成异步的
助手：可以，使用 aiohttp 替代 requests...
用户：再加个代理支持
助手：好的，添加 proxy 参数...

压缩后（150 Token）：
[历史摘要] 用户要求 Python 爬虫，已提供同步版（requests+BS4），
后改为异步版（aiohttp），最后添加了代理支持。当前状态：已完成。
```

### 3.2 层次化摘要（长文档场景）

```
完整文档（10000 Token）
    ↓
章节摘要 × N（每章 200 Token）
    ↓
元摘要（整体 300 Token）
```

**三级结构：** 完整文档 → 章节摘要 → 元摘要

### 3.3 上下文保留压缩

自动识别并保留最相关的信息，丢弃无关内容：

```markdown
原始：用户上传了 50 页 PDF，询问第 3 章和第 7 章的内容
优化：仅加载第 3 章和第 7 章的内容到上下文
节省：~80%
```

---

## 四、RAG 优化策略

### 4.1 文档处理黄金法则

| ❌ 错误做法 | ✅ 正确做法 | 节省 |
|------------|------------|------|
| 直接上传完整 PDF | 先提取关键章节 | 60-80% |
| 一次性加载全部内容 | 分块检索 + 按需加载 | 70-90% |
| 重复引用相同段落 | 引用一次 + 标记位置 | 40% |

### 4.2 分块检索策略

```markdown
❌ "分析这份 100 页文档"
→ 加载全部 100 页 → 消耗大量 Token

✅ "分析这份文档中关于性能优化的部分"
→ 检索相关章节 → 仅加载 3 页 → 节省 97%
```

### 4.3 引用优化

```markdown
❌ 重复引用完整段落（200 Token）
"根据文档第 3 章第 2 节提到的'系统性能优化包括缓存策略、数据库索引优化、异步处理...'"

✅ 引用关键句 + 位置标记（30 Token）
"根据文档第 3.2 节：缓存策略 + 索引优化 + 异步处理"
```

---

## 五、输出 Token 优化

### 5.1 输出长度控制

| 场景 | 默认输出 | 说明 |
|------|----------|------|
| 代码 | 仅关键部分 + 差异标记 | 不输出完整文件 |
| 分析 | 要点列表（≤5 条） | 不写段落 |
| 对比 | 表格（≤10 行） | 不写长文 |
| 解释 | 3 句话以内 | 按需展开 |

### 5.2 渐进式回答机制

```
第一轮：精简版（默认）→ 节省 60% 输出 Token
用户："详细点" → 展开关键部分
用户："再详细点" → 完整版
```

### 5.3 结构化输出模板

```markdown
## 代码审查
- 问题 | 行号 | 严重度 | 修复
- xxx  | 15   | 高     | xxx

## 数据分析
| 指标 | 当前值 | 变化 | 建议 |
|------|--------|------|------|
| xxx  | xxx    | +5%  | xxx  |
```

---

## 六、模型分级调度

### 6.1 任务分级策略

| 任务类型 | 推荐模型层级 | 成本对比 |
|----------|-------------|----------|
| 简单问答、翻译 | 低成本模型 | 节省 80-95% |
| 代码生成、分析 | 中等模型 | 标准成本 |
| 复杂推理、架构 | 高性能模型 | 必要投入 |

### 6.2 任务复杂度评估

```markdown
简单任务（使用精简模式）：
- 文本摘要
- 基础翻译
- 简单问答
- 格式转换

复杂任务（使用完整模式）：
- 系统架构设计
- 复杂调试
- 深度分析
- 代码审查
```

---

## 七、语义缓存

### 7.1 相似问题复用

当用户提出与之前相似的问题时，复用之前的回答，避免重复计算：

```markdown
用户："Python 怎么读取 CSV 文件"
→ 首次回答（消耗 Token）

用户："Python 读取 CSV 的方法"
→ 检测到相似问题 → 引用之前的回答
→ 节省 80% Token
```

### 7.2 缓存标记策略

```markdown
[缓存键] 问题类型 + 关键参数
[示例] "Python_CSV_read" → 所有关于 Python 读取 CSV 的问题共享缓存
```

---

## 八、Prompt 精简实战技巧

### 8.1 精简对照表

| ❌ 冗余 | ✅ 精简 | 节省 |
|---------|---------|------|
| "请帮我详细地解释一下这个代码的功能和作用是什么" | "解释代码" | 60% |
| "你能不能告诉我关于这个问题的解决方案" | "解决方案" | 55% |
| "我想请你帮我分析一下这个数据" | "分析数据" | 50% |
| "根据我刚才的描述，你觉得应该怎么做比较好" | "建议" | 60% |
| "好的，我已经理解了你的需求，现在开始执行" | "收到" | 70% |

### 8.2 结构化指令模板

```markdown
## 代码审查
输入：{代码}
输出格式：
- 问题 | 行号 | 严重度 | 修复建议

## 数据分析
输入：{数据}
输出格式：
| 指标 | 值 | 说明 |

## 文档摘要
输入：{文档}
输出格式：
- 核心观点（3条）
- 关键数据（3条）
- 结论（1条）
```

---

## 九、上下文管理清单

### 9.1 每轮对话检查

- [ ] 当前上下文是否包含已完成任务的无关信息？
- [ ] 是否可以压缩历史对话为摘要？
- [ ] 用户的问题是否可以更精简？
- [ ] 输出是否可以更结构化？
- [ ] 是否有重复内容可以利用缓存？

### 9.2 长对话管理

```
轮次 1-3：完整上下文
轮次 4-6：压缩历史，保留关键决策
轮次 7-10：仅保留最新 3 轮 + 历史摘要
轮次 10+：建议开启新对话
```

### 9.3 禁止行为

- ❌ 输出完整文件（除非用户明确要求）
- ❌ 重复解释显而易见的内容
- ❌ 使用冗长的过渡语句（"好的，我来帮你分析一下这个问题..."）
- ❌ 在上下文中保留已完成任务的细节
- ❌ 一次性加载全部文档内容

---

## 十、节省效果预估

| 场景 | 优化前 | 优化后 | 节省 |
|------|--------|--------|------|
| 日常问答 | 300 Token | 180 Token | **40%** |
| 代码审查 | 800 Token | 400 Token | **50%** |
| 文档分析 | 5000 Token | 1500 Token | **70%** |
| 长对话（10轮） | 8000 Token | 4000 Token | **50%** |
| 多文件项目 | 15000 Token | 5000 Token | **67%** |
| **综合平均** | - | - | **55%** |

---

## 十一、参考资源

- [Context Engineering: 从提示工程到上下文工程](https://blog.fsck.com/)
- [OpenAI Prompt Caching 官方文档](https://platform.openai.com/docs/guides/prompt-caching)
- [Claude Prompt Caching 官方文档](https://docs.anthropic.com/en/docs/build-with-claude/prompt-caching)
- [阿里云百炼上下文缓存](https://help.aliyun.com/zh/model-studio/context-cache)
- [OpenClaw Token 优化 10 大技巧](https://github.com/openclaw)
